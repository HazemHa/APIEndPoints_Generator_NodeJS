const fs = require("fs");

const openAPITagsController = {};

openAPITagsController.Template = () => {
  let finalFile = ``;
  let envFile = ``;
  let packageJson = ``


  return finalFile;
};

module.exports = openAPITagsController;
